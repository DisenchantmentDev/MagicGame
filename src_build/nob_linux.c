// TODO: write this full nob implementation

bool build_game(void) {
    // bool result = true;
    // Cmd cmd = {0};
    // Procs procs = {0};

    // cmd_append(&cmd, "gcc");
    return true;
}

bool build_raylib(void) { return true; }

bool raylib_exists(void) { return true; }
